name: RbSpy

on:
  push:

jobs:
  rbspy:
    runs-on: "ubuntu-latest"
    steps:
      - uses: actions/checkout@v2
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: "2.7"
      - run: apk add rbspy
      - run: rbspy record -- jekyll help
